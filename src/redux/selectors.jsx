export const selectCampers = (state) => state.campers;
export const selectFavorite = (state) => state.favorite.favorites;
export const selectIsLoading = (state) => state.camper.isLoading;
export const selectModalCamper = (state) => state.camper.modalCamper;
export const selectFilter = (state) => state.camper.filter;
export const selectError = (state) => state.camper.error;
